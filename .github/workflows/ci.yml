name: CI
on: [push, pull_request]

jobs:
  engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm, cache-dependency-path: engine/pnpm-lock.yaml }
      - run: pnpm install --dir engine
      - run: pnpm --dir engine typecheck
      # Includes the auth-guard invariant and the rule lint. If these fail,
      # nothing ships.
      - run: pnpm --dir engine test
      - run: pnpm --dir engine build

  signing:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      # Node and Swift must agree on canonical bytes. A mismatch would fail
      # every signature on device while passing both languages' own tests.
      - run: node tools/sign-bundle.mjs verify rules/*.json
      - run: swift tools/verify-canonical.swift

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: 17 }
      - run: cd android && ./gradlew :app:testDebugUnitTest :app:assembleDebug

  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd ios/NoScrollCore && swift test
      - name: Typecheck app sources against the iOS SDK
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          cd ios
          # Same file set as the NoScroll app target. Core+Web alone missed
          # SwiftUI compile errors that Xcode hits on device.
          xcrun swiftc -typecheck -target arm64-apple-ios17.0 -sdk "$SDK" \
            NoScrollCore/Sources/NoScrollCore/*.swift \
            NoScroll/App/*.swift NoScroll/Core/*.swift NoScroll/Web/*.swift \
            NoScroll/Design/*.swift NoScroll/Home/*.swift \
            NoScroll/Onboarding/*.swift
          xcrun swiftc -typecheck -parse-as-library \
            -target arm64-apple-ios17.0 -sdk "$SDK" \
            NoScrollWidget/*.swift
          # Shield sources import NoScrollCore and each other. File-by-file
          # typecheck cannot see the module or sibling types.
          MODULE_DIR=$(mktemp -d)
          xcrun swiftc -emit-module -module-name NoScrollCore \
            -target arm64-apple-ios17.0 -sdk "$SDK" \
            -parse-as-library \
            -emit-module-path "$MODULE_DIR/NoScrollCore.swiftmodule" \
            NoScrollCore/Sources/NoScrollCore/*.swift
          xcrun swiftc -typecheck -target arm64-apple-ios17.0 -sdk "$SDK" \
            -I "$MODULE_DIR" NoScroll/Shield/*.swift
          xcrun swiftc -typecheck -target arm64-apple-ios17.0 -sdk "$SDK" \
            ShieldConfiguration/*.swift
          xcrun swiftc -typecheck -target arm64-apple-ios17.0 -sdk "$SDK" \
            ShieldAction/*.swift
          xcrun swiftc -typecheck -target arm64-apple-ios17.0 -sdk "$SDK" \
            DeviceActivityMonitor/*.swift
      - name: Build the iOS app without signing
        run: |
          cd ios
          xcodebuild -list -project NoScroll.xcodeproj
          xcodebuild -project NoScroll.xcodeproj -scheme NoScroll \
            -configuration Debug -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=- \
            build
